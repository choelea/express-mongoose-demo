/*
 * Customized en messages
 * */
$.extend($.validator.messages, {
  // key: "message",
})

/**
 * i18n messages that will be used in js
 */
$.i18nMs = {
  "USER-WITH-EMAIL-EXISTS": "User with given email already exists",
}
